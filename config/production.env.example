# Production Environment Configuration
# Copy this file to production.env and fill in your actual values
# Usage: cp config/production.env.example config/production.env
# ⚠️  IMPORTANT: Use LIVE API keys - Double check before deploying!

NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# ============================================================================
# Omise API Configuration (REQUIRED)
# ============================================================================
# ⚠️  PRODUCTION: Use LIVE API keys - Handle with extreme care!
# Get your live API keys from: https://dashboard.omise.co/live/api-keys
# NEVER commit actual production keys to version control!
OMISE_PUBLIC_KEY=pkey_live_your_live_public_key_here
OMISE_SECRET_KEY=skey_live_your_live_secret_key_here
OMISE_ENVIRONMENT=production
OMISE_API_VERSION=2019-05-29
OMISE_BASE_URL=https://api.omise.co
OMISE_VAULT_URL=https://vault.omise.co
OMISE_TIMEOUT=30000
OMISE_RETRY_ATTEMPTS=3
OMISE_RETRY_DELAY=1000

# ============================================================================
# Tool Access Control Configuration (REQUIRED)
# ============================================================================
# ⚠️  PRODUCTION: NEVER use TOOLS=all - Specify exact tools only!
# Follow Principle of Least Privilege - only enable tools you actually need
# 
# Common production patterns:
# - Payment processing: create_charge,retrieve_charge,capture_charge,create_customer,create_token
# - Read-only analytics: list_charges,retrieve_charge,list_customers,retrieve_customer
# - Finance operations: list_charges,retrieve_charge,create_refund,create_transfer,list_transfers
#
TOOLS=create_charge,retrieve_charge,list_charges,create_customer,retrieve_customer

# ============================================================================
# Server Configuration
# ============================================================================
SERVER_NAME=omise-mcp-server-production
SERVER_VERSION=1.0.0
SERVER_DESCRIPTION=MCP Server for Omise Payment Integration (Production)

# ============================================================================
# Logging Configuration
# ============================================================================
# Production: Use structured JSON logging with minimal verbosity
LOG_LEVEL=warn
LOG_FORMAT=json
LOG_REQUESTS=false
LOG_RESPONSES=false

# ============================================================================
# Rate Limiting Configuration
# ============================================================================
# Production: Stricter rate limits to prevent abuse
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# ============================================================================
# Database Configuration (Optional)
# ============================================================================
REDIS_URL=redis://redis-production:6379
REDIS_PASSWORD=your_production_redis_password_here

# ============================================================================
# Monitoring Configuration (Recommended)
# ============================================================================
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090
GRAFANA_ENABLED=true
GRAFANA_PORT=3001

# ============================================================================
# Security Configuration
# ============================================================================
# Production: Strict CORS and security settings
CORS_ORIGIN=https://yourdomain.com
CORS_CREDENTIALS=true
HELMET_ENABLED=true

# ============================================================================
# Production Specific
# ============================================================================
DEBUG=false
HOT_RELOAD=false
MOCK_API=false

# ============================================================================
# Production Security Checklist
# ============================================================================
# Before deploying to production:
# ✓ Replace all placeholder values with actual production credentials
# ✓ Verify OMISE_ENVIRONMENT=production
# ✓ Verify using pkey_live_ and skey_live_ keys
# ✓ Set TOOLS to specific tools only (never use 'all')
# ✓ Set appropriate CORS_ORIGIN
# ✓ Enable rate limiting
# ✓ Set LOG_LEVEL=warn or error
# ✓ Disable DEBUG mode
# ✓ Review and test all security settings
# ✓ Set up monitoring and alerting
# ✓ Rotate credentials regularly
